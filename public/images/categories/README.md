# Category artwork

Add one PNG for each active category using the category slug as its filename:

`{category-slug}.png`

For example, a category with the slug `tote-bags` uses:

`public/images/categories/tote-bags.png`

The storefront automatically uses these images in the “Shop by Collection” section. A 4:5 portrait image is recommended; larger images are cropped to fit.