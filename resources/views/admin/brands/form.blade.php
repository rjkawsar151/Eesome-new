@extends('layouts.admin') @section('title','Brand') @section('heading','Brands') @section('content')
<h1 class="title">{{ $brand->exists?'Edit':'Add' }} brand</h1><form class="card form-grid" style="margin-top:1rem" method="POST" action="{{ $brand->exists?route('admin.brands.update',$brand):route('admin.brands.store') }}">@csrf @if($brand->exists)@method('PUT')@endif<div class="field"><label>Name</label><input class="input" name="name" required value="{{ old('name',$brand->name) }}"></div><div class="field"><label>Slug</label><input class="input" name="slug" required value="{{ old('slug',$brand->slug) }}"></div><div class="field full"><label>Description</label><textarea class="textarea" name="description">{{ old('description',$brand->description) }}</textarea></div><div class="field"><label>Order</label><input class="input" type="number" min="0" name="sort_order" required value="{{ old('sort_order',$brand->sort_order??0) }}"></div><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$brand->exists?$brand->is_active:true))> Active</label><div class="full"><button class="btn btn-primary">Save brand</button></div></form>
@endsection
