@extends('layouts.admin') @section('title','Brands') @section('heading','Brands') @section('content')
<div style="display:flex;justify-content:space-between"><div><h1 class="title">Brands</h1><p class="subtle">Organize products by maker or collection.</p></div><a class="btn btn-primary" href="{{ route('admin.brands.create') }}">Add brand</a></div><div class="card table-wrap" style="margin-top:1rem"><table class="table"><thead><tr><th>Name</th><th>Slug</th><th>Products</th><th>Status</th><th></th></tr></thead><tbody>@forelse($brands as $brand)<tr><td>{{ $brand->name }}</td><td>{{ $brand->slug }}</td><td>{{ $brand->products_count }}</td><td><span class="badge {{ $brand->is_active?'badge-green':'badge-red' }}">{{ $brand->is_active?'Active':'Inactive' }}</span></td><td><a class="btn btn-soft" href="{{ route('admin.brands.edit',$brand) }}">Edit</a><form style="display:inline" method="POST" action="{{ route('admin.brands.destroy',$brand) }}">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@empty<tr><td colspan="5">No brands.</td></tr>@endforelse</tbody></table></div>{{ $brands->links() }}
@endsection
