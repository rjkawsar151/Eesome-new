@extends('layouts.admin') @section('title','Navigation') @section('heading','Navigation') @section('content')
<div class="page-head"><div><h1 class="title">Header & footer links</h1><p class="subtle">Control public navigation without editing templates.</p></div><a class="btn btn-primary" href="{{ route('admin.navigation-items.create') }}">Add link</a></div>
<div class="card table-wrap" style="margin-top:1rem"><table class="table"><thead><tr><th>Location</th><th>Label</th><th>URL</th><th>Order</th><th>Status</th><th></th></tr></thead><tbody>@foreach($items as $item)<tr><td>{{ ucfirst($item->location) }}</td><td>{{ $item->label }}</td><td>{{ $item->url }}</td><td>{{ $item->sort_order }}</td><td><span class="badge {{ $item->is_active?'badge-green':'badge-red' }}">{{ $item->is_active?'Active':'Hidden' }}</span></td><td><a class="btn btn-soft" href="{{ route('admin.navigation-items.edit',$item) }}">Edit</a><form style="display:inline" method="POST" action="{{ route('admin.navigation-items.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-danger">Delete</button></form></td></tr>@endforeach</tbody></table></div>
@endsection
