@extends('layouts.admin') @section('title','Category') @section('heading','Categories') @section('content')<h1 class="title">{{ $category->exists?'Edit':'Add' }} category</h1><form class="card form-grid" style="margin-top:1rem" method="POST" enctype="multipart/form-data" action="{{ $category->exists?route('admin.categories.update',$category):route('admin.categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif @foreach(['name'=>'Name','slug'=>'Slug','sort_order'=>'Order','meta_title'=>'SEO title'] as $k=>$l)<div class="field"><label>{{ $l }}</label><input class="input" name="{{ $k }}" value="{{ old($k,$category->$k) }}" required>@if($k === 'slug')<small class="subtle">Use only the identifier, for example: purse</small>@endif</div>@endforeach<div class="field full"><label>SEO description</label><textarea class="textarea" name="meta_description">{{ old('meta_description',$category->meta_description) }}</textarea></div><div class="field"><label>Image</label><input class="input" type="file" name="image_upload" accept="image/*"></div><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->is_active??true))> Active</label><div class="full"><button class="btn btn-primary">Save</button></div></form>@endsection